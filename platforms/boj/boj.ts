// TODO: this might change
// TODO: put this function directly in the frontend?
export const tierBadgeUrl = (tier: string | number): string => {
  return `https://d2gd6pc034wcta.cloudfront.net/tier/${tier}.svg`;
};
