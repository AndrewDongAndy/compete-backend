// import { sendReminder } from "../mailer/mailer";

import dotenv from "dotenv";
dotenv.config();

describe("email using Nodemailer", () => {
  it.todo("can send a reminder email");
  // it("can send a reminder email", () => {
  //   sendReminder(process.env.MY_EMAIL);
  // });
});
